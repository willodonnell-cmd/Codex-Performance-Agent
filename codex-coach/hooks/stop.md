# Stop

At session end, capture a calibration record and a short coaching summary.

Template:

```text
Codex Coach closeout: completed {outcome}. Estimated {expectedCredits}; actual {actualCreditsOrUnknown}. Variance driver: {varianceReason}. Future calibration: {calibrationNote}. Remaining weekly credits: {remainingCreditsOrUnknown}.
```
